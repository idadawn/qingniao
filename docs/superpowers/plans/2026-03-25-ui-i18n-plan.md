# 实现计划：UI 国际化（i18n）

**日期**：2026-03-25
**关联设计文档**：`docs/superpowers/specs/2026-03-25-ui-i18n-design.md`

## 目标

将 Popup 和 Options 页面的所有中文文本提取到 i18n 系统中，支持中英文切换。

## 任务清单

### Phase 1: 更新 Manifest
- [x] 1. 更新 `src/manifest.json`：`action.default_title` 改为 `__MSG_extTitle__`
- [x] 2. 在 `src/_locales/zh_CN/messages.json` 添加 `extTitle`
- [x] 3. 在 `src/_locales/en/messages.json` 添加 `extTitle`

### Phase 2: Popup 页面国际化
- [x] 4. 更新 `src/_locales/zh_CN/messages.json` 添加所有 Popup 文本
- [x] 5. 更新 `src/_locales/en/messages.json` 添加上述文本的英文翻译
- [x] 6. 修改 `src/popup/popup.html`：给需要国际化的元素添加 `data-i18n` 属性
- [x] 7. 修改 `src/popup/popup.js`：添加 i18n 加载逻辑，页面加载时替换文本

### Phase 3: Options 页面国际化
- [x] 8. 更新 `src/_locales/zh_CN/messages.json` 添加所有 Options 文本
- [x] 9. 更新 `src/_locales/en/messages.json` 添加上述文本的英文翻译
- [x] 10. 修改 `src/options/options.html`：给需要国际化的元素添加 `data-i18n` 属性
- [x] 11. 修改 `src/options/options.js`：添加 i18n 加载逻辑

### Phase 4: JavaScript 中的文本国际化
- [x] 12. 检查 `src/popup/popup.js` 中的动态文本，使用 `chrome.i18n.getMessage()`
- [x] 13. 检查 `src/options/options.js` 中的动态文本，使用 `chrome.i18n.getMessage()`
- [x] 14. 检查 `src/background/background.js` 中的通知文本，使用 `chrome.i18n.getMessage()`

### Phase 5: 验证与文档
- [x] 15. 代码审查，确保所有文本都已国际化
- [x] 16. 更新 CHANGELOG.md
- [x] 17. 更新 i18n-plan.md 状态

## 实现摘要

### 新增文件
| 文件 | 说明 |
|------|------|
| `src/utils/i18n.js` | i18n 工具函数，提供 `t()` 和 `initI18n()` |

### 修改文件
| 文件 | 修改内容 |
|------|----------|
| `src/manifest.json` | `default_title` 改为 `__MSG_extTitle__` |
| `src/_locales/zh_CN/messages.json` | 添加 70+ 条中文文本 |
| `src/_locales/en/messages.json` | 添加 70+ 条英文翻译 |
| `src/popup/popup.html` | 添加 `data-i18n` 属性 |
| `src/popup/popup.js` | 引入 i18n 模块，替换动态文本 |
| `src/options/options.html` | 添加 `data-i18n` 属性 |
| `src/options/options.js` | 引入 i18n 模块，替换动态文本 |
| `src/background/background.js` | 通知使用 `chrome.i18n.getMessage()` |

### i18n Key 统计
| 模块 | Key 数量 |
|------|----------|
| Manifest | 3 |
| Popup | 20+ |
| Options | 40+ |
| Notifications | 4 |
| Common/Utils | 10+ |
| **总计** | **70+** |

## 技术方案

### i18n 工具函数

创建 `src/utils/i18n.js`：

```javascript
/**
 * 初始化页面 i18n
 * 自动替换所有带有 data-i18n 属性的元素
 */
export function initI18n() {
  // 替换 HTML 内容
  document.querySelectorAll('[data-i18n]').forEach(el => {
    const key = el.getAttribute('data-i18n');
    const message = t(key);
    if (message) {
      el.textContent = message;
    }
  });

  // 替换 placeholder
  document.querySelectorAll('[data-i18n-placeholder]').forEach(el => {
    const key = el.getAttribute('data-i18n-placeholder');
    const message = t(key);
    if (message) {
      el.placeholder = message;
    }
  });

  // 替换 title 属性
  document.querySelectorAll('[data-i18n-title]').forEach(el => {
    const key = el.getAttribute('data-i18n-title');
    const message = t(key);
    if (message) {
      el.title = message;
    }
  });
}

/**
 * 获取本地化消息
 */
export function t(key, ...substitutions) {
  return chrome.i18n.getMessage(key, substitutions) || key;
}
```

### HTML 使用示例

```html
<!-- 文本内容 -->
<span data-i18n="notConnected">尚未连接飞书</span>

<!-- placeholder -->
<input data-i18n-placeholder="appIdPlaceholder" placeholder="cli_xxxxxxxxxxxxxxxx" />

<!-- title 属性 -->
<button data-i18n-title="settingsTitle" title="设置">...</button>
```

### JavaScript 使用示例

```javascript
import { t } from '../utils/i18n.js';

// 动态文本
element.textContent = t('syncComplete');

// 带参数的文本
// messages.json: "addedX": { "message": "新增 $1$ 条" }
status.textContent = t('addedX', addedCount.toString());
```

## 注意事项

1. **占位符格式**：Chrome i18n 使用 `$1$`, `$2$` 作为占位符
2. **HTML 内容**：如果文本包含 HTML，需要在 JS 中处理，不能直接用 `data-i18n`
3. **动态内容**：JS 中动态生成的文本必须使用 `chrome.i18n.getMessage()`
4. **默认语言**：manifest 中 `default_locale` 为 `zh_CN`，所以中文是默认语言

## 参考

- [Chrome i18n API](https://developer.chrome.com/docs/extensions/reference/api/i18n)
- [Microsoft Edge 扩展 i18n](https://learn.microsoft.com/en-us/microsoft-edge/extensions-chromium/developer-guide/i18n)
